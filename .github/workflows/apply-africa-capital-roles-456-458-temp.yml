name: TEMP Apply Africa Capital Roles 456-458
on:
  push:
    branches:
      - openai/africa-capital-roles-456-458
    paths:
      - scripts/apply_africa_capital_roles_456_458_temp.py
      - .github/workflows/apply-africa-capital-roles-456-458-temp.yml
permissions:
  contents: write
jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: openai/africa-capital-roles-456-458
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install generator dependency
        run: python -m pip install --disable-pip-version-check pycountry
      - name: Apply governed capital-role corrections
        run: |
          set -euo pipefail
          python scripts/apply_africa_capital_roles_456_458_temp.py
          git diff --check
      - name: Regenerate locality catalog and audit manifests
        run: |
          set -euo pipefail
          python scripts/build_country_locality_catalog.py
          python scripts/build_country_catalog_chapters.py --all
          python scripts/verify_country_catalog_chapters.py --write-report
          git diff --check
      - name: Commit product changes and remove temporary runners
        run: |
          set -euo pipefail
          find scripts -type d -name __pycache__ -prune -exec rm -rf {} +
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm scripts/apply_africa_capital_roles_456_458_temp.py .github/workflows/apply-africa-capital-roles-456-458-temp.yml
          git add 04_World game-web scripts/build_country_locality_catalog.py
          git diff --cached --check
          git commit -m 'fix: reconcile West and East Africa capital roles'
          git push origin HEAD:openai/africa-capital-roles-456-458
